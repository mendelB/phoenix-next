/**
 * The share component accepts a style variant,
 * a custom url to share and metadata information
 * on the share location.
 */
export default from './Share';

/**
 * Full constructed <Share /> component.
 */
export ShareContainer from './ShareContainer';
